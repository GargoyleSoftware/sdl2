cc_library(
	name = "loadso",
	deps = ["//:SDL2internal"] + select({
		"//:windows": [
			"//src/loadso/windows",
		],
    "//conditions:default": [
			"//src/loadso/dlopen",
    ],
	}),
	visibility = ["//visibility:public"],
)
