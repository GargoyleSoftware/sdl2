cc_library(
	name = "main",
	deps = ["//:SDL2internal"] + select({
		"//:windows": [
			"//src/main/windows:windows",
		],
    "//conditions:default": [
			"//src/main/dummy",
    ],
	}),
	visibility = ["//visibility:public"],
)
