cc_library(
	name = "timer",
	hdrs = glob(["*.h"]),
	srcs = glob(["*.c"]),
	deps = ["//:SDL2internal"] + select({
		"//:windows": [
			"//src/timer/windows",
		],
		"//:linux": [
			"//src/timer/unix",
    ],
		"//:mac": [
			"//src/timer/unix",
    ],
	}),
	visibility = ["//visibility:public"],
)
